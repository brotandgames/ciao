<!DOCTYPE html>
<html>
  <head>
    <title>Ciao</title>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <p></p>
      <div class="row">
        <div class="col-12">
          <p>
            <%= link_to '<button class="btn btn-info">ciao</button>'.html_safe,
              root_path %>
            <%= link_to '<button class="btn btn-outline-info">
              Checks</button>'.html_safe, checks_path %>
            <%= link_to '<button class="btn btn-outline-success">
              <i class="fe fe-plus"></i> New Check</button>'.html_safe, 
              new_check_path %>
          </p>
        </div>
      </div>
      <div class="row">
        <div class="col-12">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>